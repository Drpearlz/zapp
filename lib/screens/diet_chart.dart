import 'package:flutter/material.dart';

class DietChat extends StatelessWidget {
  const DietChat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
